<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to t('app.name'), root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <% if user_signed_in? %>
        <div class="navbar-nav">
          <a class="nav-link" href="#"></a>
          <%= link_to t('views.header.profile'), user_path(current_user), class: "nav-link" %>
          <%= button_to t('views.header.logout'), destroy_user_session_path, method: :delete, class: "nav-link" %>
        </div>
      <% else%>
        <div class="navbar-nav">
          <%= link_to t('views.header.registration'), new_user_registration_path, class: "nav-link" %>
          <%= link_to t('views.header.login'), new_user_session_path, class: "nav-link" %>
      </div>
      <% end%>
    </div>
  </div>
</nav>